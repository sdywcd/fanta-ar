

server:
  port: 8501
spring:
  application:
    name: winonaapp
  datasource:
    url: jdbc:mysql://rm-uf6evwz9y63o034btlo.mysql.rds.aliyuncs.com:3306/fanta22?useUnicode=true&characterEncoding=UTF-8
    username: fanta2022
    password: 1234qwer!@#$
    driverClassName: com.mysql.jdbc.Driver
    tomcat:
      max-active: 20
      max-idle: 8
      min-idle: 8
      initial-size: 10
  jpa:
    database: MYSQL
    showSql: true
    hibernate:
      ddl-auto: update
    generate-ddl: true
    database-platform: org.hibernate.dialect.MySQLDialect

  redis:
    database: 0
    host: localhost
    port: 6379
    password:
    jedis:
      pool:
        max-active: 8
        max-wait: -1
        min-idle: 0
    timeout: 500

  data:
    redis:
      repositories:
        enabled: false
  mvc:
    view:
      prefix: /
      suffix: .html

  thymeleaf:
    cache: false
    prefix: classpath:/templates/


  servlet:
    multipart:
      enabled: true
      max-file-size: 500MB
      max-request-size: 500MB
  task:
    scheduling:
      pool:
        size: 10

